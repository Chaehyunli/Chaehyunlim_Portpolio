# CLAUDE.md

이 리포에서 작업할 때 지켜야 할 규칙. 설계 배경 전체는 [docs/superpowers/specs/2026-08-23-portfolio-site-design.md](./docs/superpowers/specs/2026-08-23-portfolio-site-design.md).

## 디자인 톤 (고정값, 바꾸지 않음)

- 무채색 3색 고정: `--color-bg`(#fff) · `--color-text`(#000) · `--color-muted`. 4번째 색을 추가하지 않는다.
- 라이트 테마 단일 고정. 다크모드 분기(`prefers-color-scheme`, `dark:` variant)를 만들지 않는다.
- 모션은 `.reveal` 클래스의 절제된 페이드/translateY 하나만 쓴다. 새 애니메이션 라이브러리(Framer Motion 등)를 들여오지 않는다.
- 여백을 넉넉히 쓴다(`--space-*` 스케일, 섹션 간 `--space-8` 기준). 밀도를 높이는 방향으로 조정하지 않는다.
- radius는 0 고정 — 각진 모서리로 통일, 텍스처 없음.

## 사이트 구조

- 단일 스크롤 페이지다. 프로젝트별로 별도 라우트를 만들지 않는다.
- 오른쪽 고정 목차 내비게이션(점 + 헤어라인 트랙, 활성 섹션만 라벨 상시 노출, 호버 시 전체 라벨)을 유지한다. 모바일에서는 숨기고 상단 진행률 바로 대체한다.

## 콘텐츠 작성 규칙 (노션 00-1 공통 작성 가이드 요약)

노션의 각 프로젝트 구성안에는 "문서용" 표(나의 역할 표, 문서 상태 표, "꼭 보여야 할 것" 표, 의도적으로 뺀 것)가 있다. **이 표들은 화면에 그대로 옮기지 않는다:**

| 구성안의 요소 | 실제 화면에서는 |
|---|---|
| 서사 한 줄 | 안 나감 — 방향을 잃지 않기 위한 내부 기준선 |
| 프로젝트 정보 표 | 헤더의 메타 한 줄 + 링크 버튼으로 압축 |
| 나의 역할 표 | 헤더의 "맡은 범위" 한 줄로 압축 (`Project.scope`) |
| 문서 상태 표 / 꼭 보여야 할 것 표 / 의도적으로 뺀 것 | 안 나감 |

문장 규칙:
- 무주어 서술 금지. "…를 두었다" → "…를 설계해 넣었다", "…가 있다" → "…를 구현했다"로 행위 동사를 쓴다.
- 수치를 쓸 때는 측정 조건을 함께 명시한다 (예: "250ms → 10ms"만 쓰지 않고 "키워드 5개 기준"을 붙인다).
- 배지는 헤더의 한 줄 정의에 이미 있는 말을 반복하지 않는다.

프로젝트별 분량(무게 배분, 임의로 늘리지 않음):

| 프로젝트 | 분량 | 주인공 |
|---|---|---|
| 모두약속 | 3장 | BYOK 보안 설계 + LLM·알고리즘 경계 |
| Masil | 3장 | AI Agent 파이프라인 + 요청 분류 설계 |
| Searchive | 2장 | 검색 병목 추적 + 태그 품질 설계 |
| PETNER | 1장 | WebSocket 세션 인증 + Soft Delete |
| 동아리모아·노소공 | 1장 (병합) | 스택과 판단 2개씩만 |

## 스타일 통합 (Tailwind v4 ↔ CSS 변수)

- `app/globals.css`의 `--color-*`, `--text-*`(폰트 사이즈), `--ease-*`, `--radius`는 Tailwind v4 `@theme` 네임스페이스와 이름이 맞으므로 그대로 선언해 `bg-bg`, `text-lg` 같은 표준 유틸리티로 쓴다.
- `--space-1..9`는 시맨틱 이름이라 Tailwind 기본 숫자 스페이싱 스케일과 이름이 충돌한다. 리네이밍하지 않고 `p-[var(--space-4)]`처럼 임의값 클래스로 참조한다.
- 새 색상·spacing 토큰이 필요하면 `app/globals.css`의 `@theme`/`:root`에 먼저 추가하고, 컴포넌트에서 하드코딩된 hex/px 값을 쓰지 않는다.

## 콘텐츠 데이터

프로젝트 콘텐츠는 `content/projects.ts`의 `Project[]`에서만 관리한다. 컴포넌트에 텍스트를 하드코딩하지 않는다.
